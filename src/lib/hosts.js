const hostile = require('hostile');

module.exports = function(env) {
  const hosts = {};

  hosts.add = (hostname) => {

  };

  hosts.remove = (hostname) => {

  };

  hosts.list = () => {

  };

  return hosts;
};
