module.exports = () => {

  const tools = {};

  tools.drush = (cmd) => {

  };

  tools.drupal = (cmd) => {

  };

  tools.wp = (cmd) => {

  };

  tools.composer = (cmd) => {

  };

  tools.platform = (cmd) => {

  };

  tools.terminus = (cmd) => {

  };

  return tools;
};
